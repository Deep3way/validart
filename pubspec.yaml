name: validifydart
description: "A library that provides a wide range of validations in Flutter. It includes validations for personal identifiers, email, URLs, numbers, financial data, and more."
version: 0.2.0
homepage: https://github.com/Deep3way/validifydart
repository: https://github.com/Deep3way/validifydart
issue_tracker: https://github.com/Deep3way/validifydart/issues

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^6.1.3
  test: ^1.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


flutter:

